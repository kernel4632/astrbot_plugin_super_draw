# 更新日志

本文件记录超级生图插件的版本变更。版本号格式为 `主版本.次版本.修订号`：
新增功能升次版本，修复问题升修订号，目录结构或大范围重构升主版本。

## [5.5.0] - 2026-09-23

### 新增

- 参考图默认不再限制数量（原来最多 8 张），新增配置项「参考图数量上限」，填 0 表示不限制。

### 变更

- 模型请求不再截断参考图（原来最多发送 16 张），统一由「参考图数量上限」控制。
- 版本号提升至 5.5.0。

## [5.4.0] - 2026-09-23

### 新增

- 重写 LLM 工具描述：`super_draw` 补充使用时机、参考图顺序、群友头像参考等说明；`super_draw_data`、`super_draw_ban` 补充 action 取值和参数格式说明。
- `super_draw` 的 `urls` 参数改为数组（`list[string]`），同时兼容逗号分隔的字符串。
- 参考图下载支持本地路径和 `file://` 地址，LLM 传入的附件路径可以直接生效。
- 参考图按内容去重，同一张图重复出现时不再占用数量上限。

### 修复

- `super_draw_data` 的 `user_key` 只接受纯数字 QQ 号，UMO、群号等非法输入会退回当前用户，避免污染积分数据。
- 工具生图成功后返回提示，引导 LLM 不要重复调用生图工具。

### 变更

- 版本号提升至 5.4.0。

## [5.3.0] - 2026-09-23

### 新增

- 引用消息中的图片在直接下载失败时，改用 OneBot `get_image` 兜底获取，兼容 `url` 为空、`file` 只是 file_id 的消息实现。
- 生图命令会从引用消息链中补充参考图 URL。
- Gemini 接口支持在供应商配置里填写自定义 `base_url`（代理地址）。
- 配置面板优化：接口类型增加中文标签和说明；生图后评价模型改为下拉选择；关闭积分制或生图后评价时自动隐藏对应字段；补充关键字段提示。

### 修复

- 每个生图任务新增总时限（超时 × 重试次数 + 60 秒），上游卡死时不再无限等待。
- LLM 数据工具 `super_draw_data` 的改分操作限制为管理员/群主，普通成员无法通过对话改分。
- `/生图改分` 数量解析增加容错，非法输入不再报错。
- 任务 ID 由 4 位提高到 8 位，并检测重复。
- 移除引用消息读取时遗留的调试日志。

### 变更

- `Point.set()` 不再调整 earned/spent 统计。
- 版本号提升至 5.3.0。

## [5.2.0] - 2026-08-30

- 识别中英文内容策略拒绝，错误信息自动脱敏。
- 限制引用消息图片遍历，避免多层嵌套。
- 完善生图错误处理与退款逻辑。

## [5.1.0] - 2026-08-25

- 源码按生图、用户、设置三个目录拆分，文件名对应具体功能。

## [5.0.0] - 2026-08-24

- 重构为 v5 架构，恢复标准编辑请求和参考图加载。
